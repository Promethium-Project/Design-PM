Le système est collaboratif -> avec un merge des représentations à une branche main 
On ne type pas les tags, un tag est un tag
## Typage des relations des tags 

- Par contre on type leurs relations ! -> c'est le truc important de ce système
- on peut avoir des liens pre-requisite
- des liens sub-topic 
- Et on peut créer des templates de liens -> qui peuvent avoir d'autres propriétés dans le reste du système. 
	- J'ai pas d'idée de plus 

## Réflexion sur le lien avec le mapping 

Donc la hiérarchie entre les topics se fait via les tags associés aux fichiers
Les liens réels [[Mapping]] ont une vocation beaucoup plus optionnelles. 



